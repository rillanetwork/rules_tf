load("@package_metadata//licenses:defs.bzl", package_metadata_license = "license")
load("@package_metadata//purl:purl.bzl", "purl")
load("@package_metadata//rules:package_metadata.bzl", "package_metadata")
load("@rules_license//rules:license.bzl", "license")

package(
    default_applicable_licenses = [":license"],
    default_visibility = ["//visibility:public"],
)

license(
    name = "license",
    copyright_notice = "Copyright 2025-2026 Rilla Network; portions Copyright Yann Degat and The Bazel Authors. See NOTICE.",
    license_kinds = [
        "@rules_license//licenses/spdx:Apache-2.0",
    ],
    license_text = "LICENSE",
    package_name = "rules_tf",
)

package_metadata(
    name = "package_metadata",
    attributes = [
        ":package_metadata_license",
    ],
    purl = purl.bazel(
        module_name(),
        module_version(),
    ),
)

package_metadata_license(
    name = "package_metadata_license",
    kind = "@package_metadata//licenses/spdx:Apache-2.0",
)

# Every file the child modules under tests/ need in order to resolve this
# module through their local_path_override. The integration tests stage these
# alongside the child module, so a package this module gains has to be added
# here as well, or the child will fail to load it.
#
# Listed rather than globbed: a glob of this package would also match the
# bazel-* convenience symlinks, and follow them into the output base.
filegroup(
    name = "local_repository_files",
    testonly = True,
    srcs = [
        "BUILD.bazel",
        "LICENSE",
        "MODULE.bazel",
        "NOTICE",
        "REPO.bazel",
        "//tf:all_files",
        "//tf/rules:all_files",
        "//tf/toolchains:all_files",
        "//tf/toolchains/git:all_files",
        "//tf/toolchains/terraform:all_files",
        "//tf/toolchains/tfdoc:all_files",
        "//tf/toolchains/tflint:all_files",
        "//tf/toolchains/tofu:all_files",
        "//tf_apply:all_files",
        "//tf_apply/rules:all_files",
    ],
    visibility = ["//tests:__subpackages__"],
)

toolchain_type(
    name = "tflint_toolchain_type",
    visibility = ["//visibility:public"],
)

toolchain_type(
    name = "tf_toolchain_type",
    visibility = ["//visibility:public"],
)

toolchain_type(
    name = "tfdoc_toolchain_type",
    visibility = ["//visibility:public"],
)
